# Data setup

Save the public Kaggle Heart Attack Risk Prediction dataset as `heart_attack_prediction_dataset.csv` in this directory. The source file is excluded from version control; this keeps the repository focused on reproducible analysis and avoids redistributing a dataset whose terms may change.

